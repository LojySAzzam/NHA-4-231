# Local backend host used by the frontend REST API service.
# Change the host/port to match your backend if needed.
VITE_API_BASE_URL=http://localhost:8000

# Optional Vite proxy target for /api routes during npm run dev.
VITE_API_PROXY_TARGET=http://localhost:8000
